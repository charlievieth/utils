syntax = "proto3";
package manifest;

import "github.com/gogo/protobuf/gogoproto/gogo.proto";

message Manifest {
  string name = 1;
  repeated string containers = 2;
  repeated string groups = 3;
}

message ManifestSlice {
  repeated Manifest manifests = 1 [(gogoproto.nullable) = false];
}
